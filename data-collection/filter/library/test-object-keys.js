/**
 * Test all of the relevant keys in a single object.
 * @param {object} item
 * @param {function} callback
 * @param {object} filters
 */

function testObjectKeys(item, callback, filters) {
	return callback(item, filters);
}

export default testObjectKeys;