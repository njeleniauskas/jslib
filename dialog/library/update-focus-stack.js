/**
 * @param {object} params - The arguments needed to pass to the stack object.
 * @param {function} callback - The function used to update the focus stack.
 */

function updateFocusStack(params, callback) {
	callback(params);
}

export default updateFocusStack;